#!/usr/bin/env ruby
require_relative '../lib/server'

port = ARGV[0] || 2000

server = Server.new(port, 'localhost')

puts "Starting server on port #{port}..."

server.start

server.join

puts 'Server has been terminated'

